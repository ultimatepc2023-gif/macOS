name: macOS Remote Desktop (RustDesk)

on:
  workflow_dispatch:

jobs:
  rustdesk-gui:
    runs-on: macos-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Admin User & Active GUI Session
        run: |
          PASSWORD="${{ secrets.RDP_PASSWORD }}"
          if [ -z "$PASSWORD" ]; then
            PASSWORD="123456"
          fi
          echo "::add-mask::$PASSWORD"

          # Create admin user
          USERNAME="abicloudpc"
          sudo dscl . -create /Users/$USERNAME
          sudo dscl . -create /Users/$USERNAME UserShell /bin/bash
          sudo dscl . -create /Users/$USERNAME RealName "Abi Cloud PC"
          sudo dscl . -create /Users/$USERNAME UniqueID 502
          sudo dscl . -create /Users/$USERNAME PrimaryGroupID 20
          sudo dscl . -create /Users/$USERNAME NFSHomeDirectory /Users/$USERNAME
          sudo dscl . -passwd /Users/$USERNAME "$PASSWORD"
          sudo dscl . -append /Groups/admin GroupMembership $USERNAME
          sudo createhomedir -c -u $USERNAME > /dev/null 2>&1 || true

          # Force GUI display/sleep prevention
          sudo caffeinate -d -i -m -u &

      - name: Install & Configure RustDesk GUI
        run: |
          # Install RustDesk
          brew install --cask rustdesk

          PASSWORD="${{ secrets.RDP_PASSWORD }}"
          if [ -z "$PASSWORD" ]; then
            PASSWORD="123456"
          fi

          # Register RustDesk Service in the system domain
          sudo /Applications/RustDesk.app/Contents/MacOS/RustDesk --install-service

          # Start server and set permanent password
          sudo /Applications/RustDesk.app/Contents/MacOS/RustDesk --service &
          sleep 5
          sudo /Applications/RustDesk.app/Contents/MacOS/RustDesk --password "$PASSWORD"

          # Fetch verified RustDesk ID
          RUSTDESK_ID=""
          for i in {1..20}; do
            RUSTDESK_ID=$(sudo /Applications/RustDesk.app/Contents/MacOS/RustDesk --get-id 2>/dev/null | grep -oE '[0-9]{9,}' | head -n 1)
            if [ -n "$RUSTDESK_ID" ]; then
              break
            fi
            sleep 3
          done

          if [ -z "$RUSTDESK_ID" ]; then
            echo "Error: Failed to register RustDesk ID."
            exit 1
          fi

          echo "RUSTDESK_ID=$RUSTDESK_ID" >> $GITHUB_ENV

      - name: Maintain Active Runner Loop
        run: |
          echo ""
          echo "======================================================="
          echo "MAC REMOTE DESKTOP READY (RUSTDESK)"
          echo "RustDesk ID : $RUSTDESK_ID"
          echo "Password    : [PROTECTED BY RDP_PASSWORD SECRET]"
          echo "======================================================="
          echo ""

          while true; do
            echo "[$(date)] Mac Runner Active..."
            sleep 300
          done
